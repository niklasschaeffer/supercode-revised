{
  "scripts": {
    "supercode:run:install": "/usr/bin/env bash ./install.sh",
    "supercode:init:memory": "npx in-memoria learn '${npm_config_folder}'",
    "supercode:run:initialize": "opencode run '/initialize --ultrathink' --agent orchestrator",
    "supercode:init": "npm run supercode:run:install && npm run supercode:init:memory --folder=${npm_config_folder} && npm run supercode:run:initialize --folder=${npm_config_folder}"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^0.15.8"
  }
}
